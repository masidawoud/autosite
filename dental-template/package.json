{
  "name": "dental-template",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "node scripts/tinacms.cjs dev -c \"astro dev\"",
    "start": "astro dev",
    "build": "node scripts/tinacms.cjs build && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.15.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.1.11",
    "tinacms": "^3.6.3"
  }
}
